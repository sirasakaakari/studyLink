<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <title>{{ config('app.name', 'Laravel') }}</title>

    <link rel="preconnect" href="https://fonts.bunny.net">
    <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />

    @vite(['resources/css/app.css', 'resources/css/common.css', 'resources/js/app.js'])
</head>
<body class="font-sans antialiased">
<div class="min-h-screen bg-gray-100">
    @include('layouts.navigation')

    {{-- header スロット --}}
    @isset($header)
        <header class="bg-white shadow">
            <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
                {{ $header }}
            </div>
        </header>
    @endisset
    <!-- ヘッダー右上 -->
<div class="relative">
    <button id="notify-btn">🔔</button>

    @if(auth()->user()->unreadNotifications->count())
        <span class="absolute -top-1 -right-1 bg-red-500 text-xs px-1 rounded-full">
            {{ auth()->user()->unreadNotifications->count() }}
        </span>
    @endif
    <ul id="notify-menu" class="hidden absolute right-0 bg-white shadow w-64">
    @foreach(auth()->user()->notifications as $notification)
        <li class="p-2 border-b {{ $notification->read_at ? '' : 'font-bold' }}">
            <form action="{{ route('notifications.read', $notification->id) }}" method="POST">
                @csrf
                @method('PATCH')
                <button type="submit" class="text-left w-full">
                    {{ $notification->data['message'] }}
                </button>
            </form>
        </li>
    @endforeach
</ul>
</div>

<script>
document.getElementById('notify-btn').onclick = () => {
    document.getElementById('notify-menu').classList.toggle('hidden');
};
</script>

    <main class="max-w-7xl mx-auto p-6">
        @yield('content')
    </main>
</div>
</body>
</html>
